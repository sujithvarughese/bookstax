import { ScrollView, StyleSheet, Text, View } from 'react-native'

const BookScreen = ({ title, author, coverImage, description, buyLinks }) => {
  return (
    <ScrollView>

    </ScrollView>
  )
}

const styles = StyleSheet.create({

})
export default BookScreen